#include "main.h"
void _puts(char *str)
	/**
	 * puts a char
	 * points to char
	 */
#include <stdio.h>
/**
* main - Entry point
*
* Return: Always 0 (Success)
*/
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
